const ItemListContainer = (props) => {
  return (
    <div style={{fontWeight: "bold", fontSize:'3rem', marginLeft:'30px  '}}>
        {props.greeting}
    </div>
  )
}

export default ItemListContainer